# TypeScript Interview Questions Blog Assignment Answer

## **Question 3: Explain the difference between `any`, `unknown`, and `never` types in TypeScript.**

TypeScript-এ এই তিনটি টাইপ ভিন্ন উদ্দেশ্যে ব্যবহৃত হয়। ভুলভাবে ব্যবহার করলে কোডে বাগ আসতে পারে বা টাইপ সেফটি নষ্ট হতে পারে। নিচে প্রতিটির ব্যবহার ব্যাখ্যা করা হলো:

---

### 🔹 **`any`**

`any` ব্যবহার করলে টাইপ চেকিং বন্ধ হয়ে যায়। যেকোনো টাইপ গ্রহণ করে এবং যেকোনো অপারেশন করা যায়। কিন্তু এতে রানটাইম বাগ হওয়ার সম্ভাবনা বেশি।

```ts
let value: any = 10;
value = "Hello"; // ✔ ঠিক আছে (any সব গ্রহণ করে)
value = true; // ✔ ঠিক আছে

value.toUpperCase();
// ❌ রানটাইমে সমস্যা হতে পারে (যদি value string না হয়)
```

---

### 🔹 **`unknown`**

`unknown` যেকোনো মান গ্রহণ করতে পারে, কিন্তু ব্যবহার করার আগে টাইপ চেক করা বাধ্যতামূলক। এটি `any` এর তুলনায় নিরাপদ।

```ts
let data: unknown = "Zeon";

// ❌ ভুল ব্যবহার
// data.toUpperCase();
// ভুল: টাইপ অজানা, তাই সরাসরি ব্যবহার করা যাবে না

// ✔ সঠিক ব্যবহার
if (typeof data === "string") {
	data.toUpperCase(); // ঠিক আছে (টাইপ যাচাই করা হয়েছে)
}
```

---

### 🔹 **`never`**

`never` এমন ফাংশনের টাইপ, যা কখনো রিটার্ন করে না। সাধারণত error throw করা বা infinite loop এর ক্ষেত্রে ব্যবহৃত হয়।

```ts
function throwError(msg: string): never {
	throw new Error(msg); // এই ফাংশন কখনো রিটার্ন করবে না
}

function infiniteLoop(): never {
	while (true) {
		// ইনফিনিট লুপ
	}
}
```

---

## **Question 5: Provide an example of using union and intersection types in TypeScript.**

TypeScript-এর Union (`|`) এবং Intersection (`&`) টাইপ কোডকে আরও শক্তিশালী এবং নমনীয় করে তোলে।

---

### 🔹 **Union Type — "একটা অথবা আরেকটা"**

Union টাইপ ব্যবহার করা হয় যখন কোনো ভ্যারিয়েবল বা ডেটা একাধিক টাইপের যেকোনো একটি হতে পারে।

**উদাহরণ: API Response**

```ts
type SuccessResponse = {
	status: "success";
	data: { title: string; id: number };
};

type ErrorResponse = {
	status: "error";
	message: string;
};

type APIResponse = SuccessResponse | ErrorResponse;

function handleResponse(res: APIResponse) {
	if (res.status === "success") {
		console.log(res.data.title); // ✔ success হলে data পাওয়া যাবে
	} else {
		console.log(res.message); // ✔ error হলে message পাওয়া যাবে
	}
}

// ❌ ভুল উদাহরণ
// handleResponse({ status: "success", message: "not allowed" });
// ভুল: success রেসপন্সে "message" property নেই
```

---

### 🔹 **Intersection Type — "সব মিলিয়ে একটাই টাইপ"**

Intersection টাইপ ব্যবহার করা হয় যখন কোনো অবজেক্টকে একাধিক টাইপের সব প্রপার্টি ধারণ করতে হবে।

**উদাহরণ: Full Stack Developer**

```ts
type Frontend = {
	frontendSkills: string[];
};

type Backend = {
	backendSkills: string[];
};

type FullStack = Frontend & Backend;

const dev: FullStack = {
	frontendSkills: ["React", "Next.js"],
	backendSkills: ["Node.js", "Express"],
};

// ❌ ভুল উদাহরণ
// const wrongDev: FullStack = { frontendSkills: ["React"] };
// ভুল: backendSkills নেই, তাই Intersection পূর্ণ হয়নি
```
